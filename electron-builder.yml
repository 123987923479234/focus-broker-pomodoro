appId: com.focusbroker.app
productName: Focus Broker
directories:
  output: release
files:
  - dist/**
  - electron/**
  - package.json
asar: true
compression: normal
npmRebuild: false
buildDependenciesFromSource: false
win:
  target:
    - target: portable
      arch:
        - x64
    - target: nsis
      arch:
        - x64
  artifactName: ${productName}-${version}-${arch}.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Focus Broker
portable:
  artifactName: ${productName}-便携版-${version}-${arch}.${ext}
